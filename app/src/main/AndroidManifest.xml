<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.joya">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Joya">
        <activity android:name=".Subject1Details" />
        <activity android:name=".coursesDetails" />
        <activity android:name=".demoRecycler" />
        <activity android:name=".course2VideoUpload" />
        <activity android:name=".course1VideoUpload" />
        <activity android:name=".codeGeneratorForStudyClass" />
        <activity android:name=".personalDetailsUpdater" />
        <activity android:name=".BasicInformationsUpdater" />
        <activity android:name=".bottomDrawer" />
        <activity android:name=".MainPage" />
        <activity android:name=".OTP_authentication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity1" />
        <activity android:name=".UserActivity" />
    </application>

</manifest>